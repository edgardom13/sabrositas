import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-programa-domicilios',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './programa-domicilios.html',
  styleUrls: ['../../styles/landing.css'],
})
export class ProgramaDomicilios {}